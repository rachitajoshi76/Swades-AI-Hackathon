{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "tsdown --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx --watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@my-better-t-app/db": "*",
    "@my-better-t-app/env": "*",
    "dotenv": "^17.2.2",
    "hono": "^4.8.2",
    "zod": "^4.1.13",
    "@hono/node-server": "^1.13.7"
  },
  "devDependencies": {
    "@my-better-t-app/config": "*",
    "@types/bun": "^1.3.4",
    "tsdown": "^0.16.5",
    "tsx": "^4.7.0",
    "typescript": "^5"
  }
}
